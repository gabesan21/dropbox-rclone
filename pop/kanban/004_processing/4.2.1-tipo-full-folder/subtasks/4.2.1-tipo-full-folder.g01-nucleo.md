# Frente F01 — Núcleo: tipo, execução, rotação e validação — [[4.2.1-tipo-full-folder]]

- **Entrega:** tipo `full-folder` executável (cópia para pasta datada + rotação por purge), aceito e validado, com spec atualizada.
- **Escopo:** dispatch, execução e rotação do novo tipo; enum e regra de max_backups; testes dessas lógicas; contrato durável na spec. Não toca restore, TUI nem docs.
- **Responsável:** agent.
- **Owns:** `executor.go`, `backup.go`, `store.go`, `executor_test.go`, `store_test.go`, `pop/specs/backup-config-e-cli.md`.
- **May read:** plano e card da task, spec atual, `restore.go` (padrão de seleção do mais recente), `tui_form.go` (uso de backupTypes), `AGENTS.md` raiz.
- **Must not edit:** `restore.go`, `restore_test.go`, `tui.go`, `tui_form.go`, `README.md`, `backups.json`, `main.go`, scripts, demais arquivos de `pop/`.
- **Depends on:** nenhuma.
- **Entrada esperada:** convenção pinada no plano — pasta remota `<base>-<AAAAMMDD-HHMMSS>/` (stamp `20060102-150405`, mesmo do compacted), prefixo `<base>-`, `rclone copy` para subir, `rclone lsf --dirs-only` (nomes com `/` final) para listar, `rclone purge` para remover excedentes; rotação de pastas é irmã da de arquivos, sem abstração compartilhada; max_backups >= 1 obrigatório em compacted e full-folder, ignorado nos demais.
- **Skills:** clean-code-change (`.agents/skills/`) — *use para planejar e executar a alteração de código*.
- **Critérios:** 1, 2, 3, 5, 7 do [[4.2.1-tipo-full-folder.plan|plano]].

## Contrato de execução

- Entregar somente o escopo e os critérios desta frente.
- **Saber parar:** no máximo 2 tentativas de fazer um critério `agent` passar quando a falha é de ambiente; na segunda, registre `ambiente`, reporte a reclassificação para `verify: user` e siga. Nunca construa infraestrutura nova só para verificar.
- Dependência ou entrada ausente/incompatível → responder `BLOCKED` ao orquestrador com evidência.
- Não implementar, simular ou corrigir dependências por conta própria.
- Não alterar caminhos fora de `Owns`; necessidade nova volta ao orquestrador.

## Resultado

- **Status:** concluída | BLOCKED.
- **Commit/artefato:** <referência>.
- **Arquivos alterados:** <lista curta, conferida contra `Owns`>.
- **Desvios:** nenhum | <desvio e autorização do orquestrador>.
- **Evidência:** <gate ou observação relevante>.
