# Julgamento — [[M-1.1-ciclo-repeticao-e-restore]]

- **Etapa:** 005_closing (ato 1) · **Responsável:** Judge Dredd (juiz único)

## Rodada 1 — 2026-08-08

- **Estratégia:** differential — não critical, primeira rodada (`pop_yolo.py verify-mode`: differential).
- **Superfície:** diff integrado vs HEAD em main (10 arquivos modificados) + `restore.go`/`restore_test.go` novos + `pop/specs/` nova; `backups.json` é gitignored e foi conferido no disco.
- **Devoluções:** execução 0 de 2 · plano 0 de 2.

### Conformidade com objetivo e specs

Os 6 pedidos do card estão no diff: `repeat_cicle` com slots `backup_time`+k·ciclo (`backup.go` `nextSlot`/`FilterDue`), `name` no schema e lookup, `force`/`restore`/`validate` no binário repassados pelo `service.sh`, `max_backups` validado e rotacionado só em `compacted` (`store.go`, `executor.go:41`), restore com `--yes` e guards.

| # | Critério | Modo | Verificação | Resultado | Evidência |
|---|----------|------|-------------|-----------|-----------|
| 1 | Ciclos 12h/3h/24h | evidência | `backup_test.go` `TestFilterDueRepeatCicle` (03:00+12h→03/15h; 3h→8 slots; default 1x/dia) + leitura de `nextSlot` | passou | testes cobrem os exemplos do card; suíte verde registrada em F01/F02/F03 |
| 2 | `validate` agrega tudo; exit 1/0 | evidência | `store.go` `ValidateBackups` + run do binário (F01: 6 problemas/exit 1; limpa/exit 0) | passou | agregação por entrada com índice/campo/motivo |
| 3 | `force <name>` com erros listando nomes | evidência | `main.go` `runForce`/`findByName`/`exitUnknownName`; run F02 (nome inexistente → exit 1 com nomes) | passou | legado sem name não endereçável, como planejado |
| 4 | `restore` exige `--yes`, guards, `.tar.gz` mais recente, sync remoto→local | evidência | `restore.go` + `restore_test.go` (guards, `pickLatestArchive`, `cleanDir`); run F02 sem `--yes` → exit 2 | passou | prefixo do archive bate com `executor.go` (`<base>-<ts>.tar.gz`) |
| 5 | `service.sh` repassa force/restore/validate | evidência | `tests/test_service.sh` testes 9–12 com binário stub; F02: 12 pass / 0 fail | passou | repasse de argumentos exato |
| 6 | TUI: `name` obrigatório, `repeat_cicle` select, `max_backups` só-compacted | evidência | leitura de `tui_form.go`/`tui.go` | passou | input com `ValidateNotEmpty`, select do enum, default 24h |
| 7 | gofmt/vet limpos | evidência | registros das 3 frentes; conferido na leitura (`gofmt -l .` vazio, `go vet` limpo) | passou | saída vazia |
| 8 | README e `backups.json` refletem o schema | evidência | diff do README (tabela, seção de operação manual, mermaid, árvore) + `backups.json` no disco | passou | exemplo com `name`/`repeat_cicle`, sem `max_backups` fora de compacted |
| 9 | Execução real force/restore contra Dropbox | humano | — | checklist humana | `verify: user` — não julgado aqui |

### Qualidade da implementação

| Severidade | Achado | Evidência | Correção necessária |
|------------|--------|-----------|---------------------|
| — | nenhum achado material | — | — |

Notas não acionáveis (não viram achado): `restore.go` novo em vez de editar `executor.go` — desvio de plano que atende ao pedido e mantém coesão; entrada com `repeat_cicle` inválido aborta o agendamento inteiro — comportamento já análogo ao de `backup_time` inválido e coberto pelo `validate`.

### Escopo e integração

- [x] Alterações respeitam `Owns` de cada frente (F01→F02 em `main.go` serializado entre ondas; F03 disjunta).
- [x] Dependências não foram implementadas oportunisticamente por consumidores.
- [x] Critérios `verify: phase` registrados na checklist da phase — nenhum neste plano (só `agent` e `user`).
- [x] Specs, documentação e contratos DOX afetados estão coerentes — spec `backup-config-e-cli` (draft) bate com o diff; promoção para active é passo do orquestrador no fechamento (sync-specs).

## Veredito

- **Decisão:** aprovada → entrega e encerramento.
- **Bloqueantes:** nenhum.
- **Defeito de plano:** nenhum.
- **Sugestões/nits:** nenhum útil.
- **Checklist humana:** critério 9 (`verify: user`) — executar `./service.sh force <name>` e `./service.sh restore <name> --yes` contra o Dropbox real no servidor; pass é backup forçado e pasta restaurada. Execução real destrutiva não foi feita pelos executores (correto: rclone presente, mas restore apaga conteúdo local).
- **Resumo:** os 6 pedidos do card estão implementados no binário, no `service.sh`, na TUI e na documentação, com testes Go e shell novos verdes por evidência registrada e diff coerente com a spec; nada material a ajustar.

<!-- pop-verdict round=1 decision=aprovada -->
