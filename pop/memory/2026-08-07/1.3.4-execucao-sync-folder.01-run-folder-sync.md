---
task: 1.3.4-execucao-sync-folder
entry: 01-run-folder-sync
---

# Implementação do backup folder-sync

Criada função `RunFolderSync` em `executor.go`: chama `rclone bisync` para sincronização bidirecional, com orientação de `--resync` na primeira execução quando necessário.

## Evidência

- [[executor.go]] — *o arquivo onde a mudança está*.
