---
task: 3.1.1-readme-completo
entry: 01-readme
---

# README completo do projeto

`README.md` reescrito (antes só o título) na ordem de uso real: visão geral do motor (timer dispara o binário, que executa os `backup_time` na janela `[agora, agora+intervalo)`), instalação com detecção de distro (apt/pacman/dnf), duas rotas de token Dropbox headless (port-forward `ssh -L 53682` e `rclone authorize "dropbox"` cobrindo o RECON NEEDED da Epoch 1), tabela de campos do `backups.json` com a semântica dos 3 tipos (`compacted` com rotação por `max_backups`, `folder-backup` como `rclone sync`, `folder-sync` como `rclone bisync` exigindo `--resync` na 1ª vez), TUI `manage` e `service.sh` com nota de `loginctl enable-linger`.

## Evidência

- [[README]] — *o documento está aqui*.
