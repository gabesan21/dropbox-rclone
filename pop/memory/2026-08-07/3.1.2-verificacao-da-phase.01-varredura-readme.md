---
task: 3.1.2-verificacao-da-phase
entry: 01-varredura-readme
---

# Varredura README × artefatos

Cada comando, flag, caminho e valor citado no `README.md` foi conferido contra a fonte: `install.sh` (distros/pacotes), `setup-rclone.sh` (remote default `dropbox`), porta 53682 do OAuth rclone, `rclone authorize`/`sync`/`bisync --resync`, struct do `backups.json` (`backup.go:11-18`), nome datado do `.tar.gz` e rotação (`executor.go`), CRUD da TUI com validação (`tui.go:90`, `tui_form.go`), ações do `service.sh` e units em `~/.config/systemd/user/`, default 30 do intervalo (`main.go`, `service.sh`), `.env`/`backups.json` no `.gitignore`. Resultado: zero divergências — README aprovado como fonte da validação humana da Phase 3.2.

## Evidência

- [[README]] — *documento auditado, sem alteração*.
