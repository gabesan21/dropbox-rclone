---
task: 3.1.2-verificacao-da-phase
entry: 01-varredura-readme
---

# Varredura README × artefatos

Cada comando, flag e caminho do `README.md` conferido contra a fonte: `install.sh`, `setup-rclone.sh`, porta 53682, `rclone authorize`/`sync`/`bisync --resync`, struct do `backups.json`, rotação do `.tar.gz` (`executor.go`), CRUD da TUI, ações do `service.sh`, default 30 do intervalo, `.env`/`backups.json` no `.gitignore`. Zero divergências — README aprovado como fonte da validação humana da Phase 3.2.

## Evidência

- [[README]] — *documento auditado, sem alteração*.
