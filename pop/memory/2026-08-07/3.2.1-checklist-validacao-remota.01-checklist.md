---
task: 3.2.1-checklist-validacao-remota
entry: 01-checklist
---

# Checklist de validação em servidor remoto

Roteiro com caixas `[ ]` e "pass é" observável por item, derivado do README sem duplicar explicações: clone, `./install.sh`, `go build`, token Dropbox headless (com coleta de qual rota funcionou — port-forward 53682 × `rclone authorize` — fechando o RECON NEEDED da epoch), `setup-rclone.sh`, `.env`/`backups.json` via TUI `manage` com uma entrada de cada tipo, execução manual verificando compacted (rotação), folder-backup (espelho) e folder-sync (`--resync` inicial), `service.sh enable` + `enable-linger` + `journalctl`, e limpeza. Seção Resultado registra data, servidor, rota do token e falhas — é o instrumento da task 3.2.2 (gate humano).

## Evidência

- [[checklist-validacao-remota]] — *o checklist está aqui*.
