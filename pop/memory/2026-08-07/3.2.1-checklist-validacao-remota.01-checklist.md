---
task: 3.2.1-checklist-validacao-remota
entry: 01-checklist
---

# Checklist de validação em servidor remoto

Roteiro com caixas `[ ]` e "pass é" por item, derivado do README: clone, `install.sh`, `go build`, token headless (coleta qual rota funcionou — fecha o RECON NEEDED), `setup-rclone.sh`, `.env`/`backups.json` via TUI `manage`, execução manual dos 3 tipos (rotação, espelho, `--resync`), `service.sh enable` + `enable-linger` + `journalctl`, limpeza. Seção Resultado registra data, servidor, rota e falhas — instrumento da task 3.2.2 (gate humano).

## Evidência

- [[checklist-validacao-remota]] — *o checklist está aqui*.
