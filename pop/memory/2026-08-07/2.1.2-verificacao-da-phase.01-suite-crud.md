---
task: 2.1.2-verificacao-da-phase
entry: 01-suite-crud
---

# Suíte de testes do CRUD e da validação

`store_test.go` cobre o round-trip `SaveBackups`→`LoadBackups` em arquivo temporário (incluindo newline final) e o `ValidateBackup` em tabela: os 3 tipos válidos e 9 violações (obrigatórios vazios, HH:MM malformado, max_backups < 1, type desconhecido). Nenhum defeito foi encontrado — o código de produção ficou intacto. Suíte Go completa, `go build`, `go vet` e as suítes shell da Epoch 1 (6 e 7 pass) verdes.

## Evidência

- [[store_test.go]] — *a suíte nova está aqui*.
- [[store.go]] — *o código sob teste*.
