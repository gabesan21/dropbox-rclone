---
task: 4.2.2-verificacao-da-phase
entry: 02-filtro-stamp-e-regra-agregada
---

# Filtro exato de stamp e regra agregada de max_backups

`TestStampDatado` fixa em teste de tabela a decisão do reparo 04 da 4.2.1: só `AAAAMMDD-HHMMSS` completo entra na rotação — `.tar.gz`, stamp parcial e lixo ficam de fora. `TestValidateBackupsMaxBackupsFullFolder` prova que a regra `max_backups >= 1` de full-folder propaga na rota agregada, com problema marcado no campo e no índice certos. Completam a cobertura do critério phase 3 junto de `TestPastasExcedentes` e `TestRunFullFolderRotacao`.

## Evidência

- [[executor_test.go]] — *`TestStampDatado`, tabela de aceites/rejeições*.
- [[store_test.go]] — *caso agregado full-folder em `ValidateBackups`*.
