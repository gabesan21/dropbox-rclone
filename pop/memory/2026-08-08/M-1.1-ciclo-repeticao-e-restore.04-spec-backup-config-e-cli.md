---
task: M-1.1-ciclo-repeticao-e-restore
entry: 04-spec-backup-config-e-cli
---

# Spec do schema e da CLI

A coleção `pop/specs/` nasceu nesta task com o INDEX e a spec `backup-config-e-cli`: contrato do formato do `backups.json` (campos, enum de ciclos, invariantes de unicidade de `name` e de `max_backups` só-compacted) e da superfície de comandos do binário e do `service.sh` (`force`, `restore --yes`, `validate`). Criada como draft no 002; a promoção para active acompanha o fechamento da task.

## Evidência

- [[pop/specs/backup-config-e-cli|backup-config-e-cli]] — *a spec criada*.
- [[pop/specs/INDEX]] — *índice da coleção nova*.
