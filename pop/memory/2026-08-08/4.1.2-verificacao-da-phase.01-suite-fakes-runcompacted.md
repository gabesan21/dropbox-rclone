---
task: 4.1.2-verificacao-da-phase
entry: 01-suite-fakes-runcompacted
---

# Suíte da phase 4.1 com binários fake

`executor_test.go` (370 linhas) prova o `RunCompacted` sem Dropbox real: scripts shell `tar`/`rclone` num `bin/` de `t.TempDir()` no PATH via `t.Setenv` (sem `t.Parallel`), remoto fake local e log de invocações para asserts de "não aconteceu". Cobre aborto em origem inválida/ilegível (skip sob root), erro não engolido pelo pipe, remoção do parcial, sucesso com `.tar.gz` datado genuíno e rotação. Re-run do gate: 7/7 PASS, gofmt/vet limpos; nenhum defeito revelado.

## Evidência

- [[executor_test.go]] — *a suíte inteira da checklist da phase 4.1*.
- [[pop/specs/backup-config-e-cli|backup-config-e-cli]] — *siga para o contrato do compacted que a suíte prova*.
