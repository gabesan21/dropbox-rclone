---
task: 4.2.2-verificacao-da-phase
entry: 01-restore-fullfolder-ponta-a-ponta
---

# Restore full-folder ponta a ponta

`TestRestoreFullFolder` e `TestRestoreFullFolderSemPasta` exercitam o caso full-folder do switch de `RunRestore`: o primeiro prova limpeza do local + cópia da pasta datada mais recente (lixo fora do padrão ignorado, log comprova `lsf --dirs-only` + `copy`); o segundo prova erro sem destruir o destino quando não há pasta do prefixo. O `rcloneFake` ganhou decisão de direção do `copy` pelo prefixo `conta:` no 1º argumento — reversível, sem quebrar os testes de backup.

## Evidência

- [[restore_test.go]] — *os dois testes novos do restore full-folder*.
- [[executor_test.go]] — *o rcloneFake com copy bidirecional*.
