---
task: 4.2.1-tipo-full-folder
entry: 03-tui-e-documentacao
---

# TUI e documentação reconhecem full-folder

O select de tipo da TUI deriva de `backupTypes` (sem mudança de código); a descrição do campo max_backups passou a citar compacted e full-folder. O README registra o 4º tipo na tabela de campos, na seção de tipos, no restore e no exemplo de `backups.json` (arquivo gitignorado — o exemplo versionado vive no README e a cópia local recebeu a entrada).

## Evidência

- [[README.md]] — *tabela, seção de tipos, restore e exemplo* (commit c8ca827).
- [[tui_form.go]] — *texto do campo max_backups*.
